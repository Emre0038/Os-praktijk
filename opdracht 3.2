#include <stdio.h>
#include <string.h>

int main()
{
    char sen[10];
    printf("Type hier de zin dat je in gedachte hebt alstublieft: ");

    gets(sen);
    
    printf("Je hebt  %d karakters ingevoerd.", strlen(sen));
    
    return 0;
}
